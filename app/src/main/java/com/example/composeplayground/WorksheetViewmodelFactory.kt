package com.example.composeplayground

import android.content.Context
import androidx.annotation.MainThread
import androidx.fragment.app.Fragment
import com.example.composeplayground.data.WorksheetRepository
import androidx.lifecycle.*

//@Suppress("UNCHECKED_CAST")
//class WorksheetViewmodelFactory(val repository: WorksheetRepository) : ViewModelProvider.Factory {
//    override fun <T : ViewModel> create(modelClass: Class<T>): T {
//        return modelClass.getConstructor(WorksheetRepository::class.java).newInstance(repository)
//    }
//}
